server:
  port: 8855

spring:
  ai:
    mcp:
      server:
        name: my-tools-server
        version: 1.0.0
        type: SYNC
        # 使用 SSE 协议，供 ch08-mcp-client 通过 HTTP 连接
        protocol: SSE
        capabilities:
          tool: true

logging:
  level:
    io.modelcontextprotocol: DEBUG
