<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.javalearn</groupId>
    <artifactId>spring-ai-study</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <modules>
        <module>ch01-chat-demo</module>
        <module>ch02-embeddings-demo</module>
        <module>ch03-key-polling</module>
        <module>ch04-function-call</module>
        <module>ch05-chat-memory</module>
        <module>ch06-advanced-tool-call</module>
        <module>ch07-rag-knowledge</module>
        <module>ch08-mcp-client</module>
        <module>ch09-mcp-server</module>
        <module>ch10-multi-agent</module>
    </modules>

    <properties>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <spring-ai.version>2.0.1</spring-ai.version>
        <spring-boot.version>4.2.0-M1</spring-boot.version>
    </properties>


    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <!-- JDK 23+ 的 javac 默认不再隐式运行注解处理器，
                         不显式开启会导致 Lombok 静默失效（getter/setter 不生成） -->
                    <proc>full</proc>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
